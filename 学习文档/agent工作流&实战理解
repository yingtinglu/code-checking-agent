Agent：LLM的代理
行动框架：ReAct模式  思考-行动-观察-结束
根据用户的任务对任务进行拆解，分析执行步骤；按步骤执行，如果需要外部的工具帮助，就会调用相应的工具，根据工具执行的结果再次进行分析，直到回答满意，再返回给用户
（1）用户提问 - Agent
	根据用户输入的用户级提示词，去分层加载系统提示词（基础角色、系统环境、所处位置、用户持久要求），组装提示词
	Agent内置了一些工具、skills，把工具对应的json文档和skill的列表（名称和描述）一起发给LLM
	另外，agent可能还需要调用一些外部的工具接口（API），这些工具是由第三方提供的，一般以HTTP API的形式存在，为了统一对外服务、标准化暴露工具，产生了MCP协议，该协议对工具暴露制定了标准化的要求，存在MCP Client（对接agent）和MCP Server（对接工具服务），对HTTP API进行了一层封装
（2）LLM – Agent – 工具 / skills
	LLM会进行思考，当能力不足时，会查阅是否存在外部的工具或skill可以帮忙解决问题：如果存在工具，可以通过function calling发送统一的json格式的文件进行工具调用，这里工具调用存在统一的输入输出格式和一些约束条件，这样可以使流程更加规范化，LLM更容易理解和执行；如果存在skill，可以去检索目录看是否存在匹配的skill，如果存在，就会要求agent把对应的完整skill文件发过来【按需加载】，后续LLM会根据skill中的指令来进行执行。
Skill：元数据层-指令层-资源层  渐进式披露，是可复用的说明文档
（3）Agent - LLM
	Agent会把LLM要求的工具执行结果和skill文档返回给LLM，LLM会根据工具执行结果和文档来思考下一步应该做什么，这里的工具包括联网搜索、文件读写、RAG【检索增强生成，离线+在线，向量库】，skill包含一些约定俗成的技能步骤，这部分内容相当于给LLM拓展了一些行动能力，让LLM这一大脑接入了四肢，可以真正的做一些事情，事情做完后，LLM还会去观察做的对不对，去修正行为，LLM持续和agent智能体协作，反复循环这一流程，直到LLM判断回答/执行结果已经满意，才会把结果返回给用户。
LLM在其中扮演着决策者（大脑）的角色
RAG：知识库，提供实时信息/私有数据，解决LLM幻觉问题

上下文压缩：以上的很多信息都会被存储在LLM的上下文中，但上下文的大小是有限的，当上下文达到一定大小后，就会触发上下文的压缩（总结汇总），此时有可能会丢失部分重要信息

为了保证agent系统的正常运转，还会在agent外围设置Harness防护网，进行格式清洗、错误重试、参数校验、输入输出信息管理、机械拦截等操作，因为LLM有时候会偷懒，不会按照设定的格式进行工具调用、参数传递等，我们需要在中途进行检查和校验，如果LLM结果不合格，要让LLM重新生成，以防系统报错，另外出于安全性的考虑，必须对用户的输入和系统的输出进行检查，以防用户进行恶意代码攻击、系统给出不好的回答（例如如何制作原子弹），这些内容都应该被拦截。

多agent协作：可以存在主agent和子agent，多agent并发执行，提高效率

实战：以灵犀平台为例
（1）多agent协作：主agent（code agent，Q&A agent）+子agent（plan agent，explore agent，general agent）
	Code agent：编程助手，可以对项目文件进行读写操作
	Q&A agent：问答助手，主要用于帮助理解分析代码，只可以进行读操作
	Plan agent：设计实施计划，评估调整策略
	Explore agent：搜索、查阅、分析代码
	General agent：为项目的实施提供读写操作
系统定义了多agent并发总数的上限，以及单类型子agent并发数的上限
（2）系统提示词
为每个agent都设计了一套系统提示词，主要包含核心原则、问题解决、代码质量、用户交流、安全性、工具（内置工具）及其详细描述
（3）工具
是否赋予该agent使用内置工具的权限
读文件、创建和编辑文件、关键词搜索、执行脚本的权限、向用户询问、文档转换（把pdf、doc、ppt、xls等转换为md）
（4）skills
设置skills加载路径
（5）MCP
区分本地模式和远端模式
执行一个mcp前，需要先确认该mcp的依赖已经被安装：uvx xxx / npx xxx
（6）记忆
可以从文件、文件夹中加载记忆【参考内容：项目背景、项目说明文档等】
预加载→系统提示词
模型本身是无状态的，agent必须把外部记忆重新注入到当前上下文中，模型才能在这一轮推理中使用它。这里放在系统提示词中，是为了提高这些记忆作为背景约束【用户长期约束】的优先级和稳定性。
模型在进行每一轮推理时，都会携带本轮次的上下文信息，每一轮的上下文都会发生改变，上下文信息是动态的、不断更新的
模型上下文：系统提示词，用户输入、历史对话、记忆、工具结果、代码内容、模型输出（临时的）
好的agent不会把记忆全部放入系统提示词中，而是应该先做相关性检索、可信度分层、冲突处理、过期清理、压缩等操作，把真正需要的内容加载到系统提示词中，其他的也可以放入上下文中，但一般来说，系统提示词的优先级是最高的，可以保证稳定的被模型读取
（7）压缩
当上下文达到总存储容量的一定比例时（可设置），就会触发压缩机制
压缩机制：把完整的对话和工具调用过程保存在session中，但给LLM看的时候，会把对话总结成摘要块，把旧的工具调用和当前工具过程都替换为短摘要，从而减少上下文占用
 	逻辑删除：不传给LLM，但文件中仍然保留
 	摘要块：把整轮历史折叠为短记录
 	Last words：压缩时工具过程结束后的简短交接说明
（8）其他功能
对话历史存储、恢复，对话切换，模型选择，日志


Claude Code源码分析
1、系统提示词
由一个关键常量 SYSTEM_PROMPT_DYNAMIC_BOUNDARY 把提示词切分为静态部分和动态部分，静态部分：所有用户共享，可缓存，省钱省时间，动态部分：每个用户独立加载，可以记住用户的偏好，保证个性化
可借鉴的设计：如果你在做一个 AI 应用，可以在 system prompt 里区分「可缓存的通用规则」和「用户特定的上下文」，前者集中处理，后者按需加载。
2、Auto模式：权限分类器（子AI）
主 AI 每次想执行操作，都会经过一个独立的权限分类器。这个分类器有自己的 system prompt，用于做安全审核，分三档判断：
	Allow：直接放行
	Soft Deny：需要用户确认
	Hard Deny：直接拦截
这里存在四层安全流水线，分别为：规则、风险、白名单和AI审查
	第一层：查已有规则，命中就放行
	第二层：低风险操作直接跳过
	第三层：白名单放行只读工具
	第四层：调用独立 Claude Sonnet 做安全分类
分类器温度设为 0，确保确定性输出。还有熔断机制：连续 3 次被拒或累计 20 次被拒后，自动降级为手动确认模式。
可借鉴的设计：给 AI 的操作权限分层处理，而不是一刀切全放行或全禁止。越危险的操作用越重的验证手段。
3、记忆系统
会记住你的偏好，不记代码等会发生改变的内容，如果记录了这些可能发生改变的内容，下一次对话时该内容发生了变化，那么记忆中的相关内容就会对 AI 产生误导。
记忆提取不是每条消息都触发的，而是存在限流机制，由独立的 fork agent 完成，每N轮去检查一次记忆，在对话轮次比较多时，模型可能会忘记之前的记忆，因此此时有必要对记忆进行重新加载。这个子 agent 继承了主对话的上下文，但只能进行读文件和写入记录目录两个操作。
提取的记忆主要分为四类：用户的偏好、行为的反馈、项目信息和外部资源引用，且存在一个机制：autoDream，会自动整理你的记忆文件。
可借鉴的设计：如果你做需要记忆的 AI 应用，明确区分「会过时的事实」和「不会过时的偏好」，前者实时读取，后者才存入记忆。
4、上下文压缩：9段式结构化提取
核心请求、关键概念、文件和代码、错误和修复、解决过程、所有用户消息（必须完整保留）、待办任务、当前工作、下一步行动
所有用户消息必须完整保留。用户在第 3 轮纠正了一个做法，如果压缩时丢掉那条纠正，后续就会重蹈覆辙。
可借鉴的设计：对话压缩要有明确的结构化格式，而不是自由总结。并且明确哪些信息必须保留（用户纠正、关键偏好）vs 可以丢弃（中间推导过程）。
5、搜索：用grep而不是RAG
当你有一个足够聪明的大脑（LLM）理解搜索结果时，不需要一个很聪明的搜索引擎。grep 给精确匹配，LLM 来理解结果之间的关系。
与其让每个环节都变复杂，不如让一个环节足够强，其他环节保持简单。
可借鉴的设计：在 AI 应用里，不是每个环节都需要 AI。某些简单重复的工作（搜索、排序、过滤）用传统方法反而更高效，让 AI 做它真正擅长的事。

这份泄露的源码是claude code的核心客户端逻辑，是本地CLI客户端，负责：接收用户输入、编排LLM对话（构建prompt、调用API、处理流式响应）、执行工具操作、管理权限（每次工具调用前的用户审批）、渲染终端UI
作为一个agent系统，这里缺失的就是模型部分，包含的是除了大脑以外的一切——手脚（工具）、记忆（上下文）、安全机制（权限）、协调能力（多agent）
这里所有的“智能”都来自于API返回，本地只做编排和执行，也就是说模型是决策者，本地客户端是执行者
