长期记忆流程实现
所有代码文件写在chat_agent/memory/，除了在graph.py中加入load_memory节点和push_queue节点，加入两条条件边

1. 加载记忆 （load_memory节点）
在user_message中追加任务：读取当前项目的记忆索引文件，选择最多5条与当前用户分析要求相关性最强的5条记忆（排除与当前时间时差超过48小时之前的记忆）。
2. fork agent （提取记忆和存储记忆）
ChatAgent 实例持有一个内存队列和一个 daemon 后台线程，一次只处理一个任务，顺序执行。
  入队（push_queue节点）：主 Agent 每轮 save_response 完成后，如果 current_project 不为 None，深拷贝list(context_manager._api_messages[_cur_turn_start:])截取本轮对话片段，深拷贝，连同current_project、model、temperature、max_tokens 一起作为快照放入队列（这些内容一起是一个快照）。

  后台线程处理流程：
（1）fork_build_context：从队列中的快照构建 LLM 上下文。
上下文：  - api_messages 当前轮片段（从 _cur_turn_start 截取，深拷贝）
  		  - model、temperature、max_tokens
读取 prompts/memory_extraction.txt 作为系统提示词；读取current_project记忆文件夹里的memory_index.md 的内容（记忆文件路径列表）（告诉LLM现有记忆，不要重复或者在什么记忆上更新），拼接在系统提示词之后；然后将快照中的本轮 api_messages（system/user/assistant/tool 四种角色）追加上去，对tool内容截断到 200 token，总计不超过 1000 token，超出的从最早开始丢弃tool。
（2）fork_call_LLM：用快照中的 model、temperature、max_tokens 调用非流式 LLM，tools=None，HTTP 超时 60 秒。LLM 根据系统提示词规则和本轮对话内容，输出 JSON
格式的记忆列表（每条包含 action=create/update、category、title、content、scope），无值得提取时返回空列表。
（3）write_and_search_memory：解析 JSON，action=create 在 current_project/.agent_memory/{category}/ 下新建 md 文件并在索引中追加地址；action=update 直接更新已有文件的内容。对本轮写入的每条记忆，查索引文件中同 category 下是否有标题 normalize（去空格、标点、转小写）后重复的，有则合并（保留内容更丰富的版本），无则保留新写入的。

  异常处理：每个任务整体包裹在 try/except 中，单次任务失败时重试，最多 3 次，3 次仍失败则打印日志跳过。后台线程永远不会因为异常而死。
  关闭：后台线程为 daemon 线程，主线程退出时自动终止。
存储格式：
current_project / .agent_memory
  # memory_index.md
  ## user_feedback
  - user_feedback/忽略generated目录.md
  ## model_insight
  - model_insight/Controller不直接调Repository.md
3. LangGraph
START
→ append_user_msg（输入防护 + 追加用户消息到上下文）
→ allowed_path里有目录：load_memory → build_context
→ allowed_path里没有目录：build_context
    → call_llm（调 LLM）
    → route_response（条件分支）
        ├─ execute_tools 
→ add_allowed_path： load_memory → build_context
→ 不是add_allowed_path：build_context
        ├─ final_answer → save_response → push_queue → END
        └─ save_response → push_queue → END
注：allowed_path只保留一条，add_allowed_path之前清空allowed_path
fork agent（后台独立运行）：
  START → fork_build_context → 追加user_message（选取并写记忆的任务）→ fork_call_LLM 
→（LLM评估已经提取完并写入所有记忆）发消息告诉主Agent
→（LLM评估还没有完成提取并写入所有记忆）write_and_search_memory → fork_call_LLM
