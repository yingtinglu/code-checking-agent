# 代码仓库质量分析系统

## 项目介绍

基于大语言模型构建的代码质量分析系统，支持远程质量平台数据聚合分析与本地代码仓库静态分析两种模式，可自动诊断代码退化问题并生成结构化分析报告。负责系统整体架构设计与核心模块开发，重点解决长任务可靠性、资源容量治理、工具调用安全与稳定性保障等工程问题。

## 功能亮点与技术实现

- **工作流编排与任务调度**：基于状态机实现多轮工具调用的工作流编排，采用"计划—执行—反思"循环支持任务计划的动态修订与强制收尾，保证长流程可预测收敛，避免死循环与资源耗尽。

- **任务持久化与故障恢复**：基于 checkpoint 机制实现任务节点级状态持久化，崩溃或中断后可从断点重建上下文并续跑未完成任务，避免长流程从头重跑，提升系统可靠性。

- **资源容量治理**：设计分层资源压缩策略——大对象旁路存储按需取回、历史数据摘要压缩、冗余数据按轮次淘汰，在有限资源窗口内支撑长流程稳定运行，兼顾容量与数据完整性。

- **稳定性与防重保障**：针对工具调用失败实现错误分类重试（瞬时错误退避重试、永久错误快速失败）与冷却机制（连续失败自动屏蔽、恢复后自动解禁）；基于参数指纹的调用去重与次数预算控制，防止重复调用与循环执行。

- **数据存储与缓存**：基于 SQLite 实现数据快照缓存，采用复合主键索引、TTL 过期淘汰与版本控制，避免相同参数重复请求外部接口；数据文件采用原子写入（先写临时文件再替换），保证异常下文件不损坏。

- **权限管控与安全防护**：实现文件操作路径白名单（默认拒绝、最小授权、防路径遍历）与分级授权网关（高风险操作交互确认）；命令执行采用只读白名单与危险操作硬禁策略；输入输出双向校验（注入拦截、敏感信息脱敏、凭证泄露与配置泄露防护）。

- **统一接入层设计**：设计统一工具注册表，屏蔽本地工具与外部协议工具的异构差异，对外提供标准化调用接口与统一分派执行，支持工具动态注册与扩展。

- **可运维性**：实现按会话维度的文件化日志体系，调试信息落盘存储不污染业务输出，支持按会话回溯定位问题。

## 技术栈

Python / LangGraph / MCP / OpenAI SDK / SQLite / httpx
